# Copia questo file in 'config.env' e compila i valori.

# L'URL SSH o HTTPS del repository GitHub pubblico che userai per lo stato della rete.
# Esempio: https://github.com/tuo-utente/synapse-state.git
SYNAPSE_GIT_REPO_URL=

# Un nome unico per il tuo nodo. Se non specificato, ne verrà generato uno casuale.
# Esempio: node-alpha
SYNAPSE_NODE_ID=
